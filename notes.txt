Common Element Selectors:
    # By ID
    element = driver.find_element(By.ID, "username")

    # By Name
    element = driver.find_element(By.NAME, "email")

    # By CSS Selector
    element = driver.find_element(By.CSS_SELECTOR, ".submit-button")

    # By XPath
    element = driver.find_element(By.XPATH, "//button[@type='submit']")

Common Actions
    # Type text
    element.send_keys("text here")

    # Click
    element.click()

    # Clear existing text
    element.clear()

    # Get text content
    text = element.text

    # Submit a form
    element.submit()

TO-DO:
- replace time.sleep() with a dynamic wait
- make inputs such as hours, times, days work, to make script usable for anyone